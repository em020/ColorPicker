apply plugin: 'com.android.library'

ext {
  bintrayRepo = 'repo1'
  bintrayName = 'modified-jaredrummler-ColorPicker'

  publishedGroupId = 'com.artdenshop.jrcolorpicker'
  libraryName = 'ColorPicker'
  artifact = 'library'

  libraryDescription = 'modified-jaredrummler-ColorPicker'

  siteUrl = 'https://site-url.foo'
  gitUrl = 'https://git-url.foo'

  libraryVersion = '0.0.2'

  developerId = 'em020'
  developerName = 'Yimin S'
  developerEmail = 'eamyn.s@gmail.com'

  licenseName = 'The Apache Software License, Version 2.0'
  licenseUrl = 'http://www.apache.org/licenses/LICENSE-2.0.txt'
  allLicenses = ["Apache-2.0"]
}

tasks.withType(JavaCompile) {
  options.encoding = "UTF-8"
  javadoc {
    failOnError false
    options{
      encoding "UTF-8"
      charSet 'UTF-8'
//            author true
//            version true
//            links "http://docs.oracle.com/javase/7/docs/api"
//            title "fragment-view-pager docs"
    }
  }
}

android {
  compileSdkVersion 25
  buildToolsVersion "25.0.2"
  resourcePrefix "cpv_"

  defaultConfig {
    minSdkVersion 14
    targetSdkVersion 25
  }

  lintOptions {
    abortOnError false
  }
}

dependencies {
  compile 'com.android.support:appcompat-v7:25.3.1'
}

apply plugin: 'com.getkeepsafe.dexcount'

//apply from: rootProject.file('gradle/maven-push.gradle')


// Place it at the end of the file
apply from: 'https://raw.githubusercontent.com/nuuneoi/JCenter/master/installv1.gradle'
apply from: 'https://raw.githubusercontent.com/nuuneoi/JCenter/master/bintrayv1.gradle'